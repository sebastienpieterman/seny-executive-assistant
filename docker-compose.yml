services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: seny
      POSTGRES_PASSWORD: seny_dev
      POSTGRES_DB: seny
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
